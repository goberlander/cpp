struct ListNode
{
    int(or any data type) value;
    ListNode *next; // To point to the next value
}

ListNode *nodePtr; // Can be used to traverse a Linked List. Linked List is just a data structure that uses pointers for manipulating the data.